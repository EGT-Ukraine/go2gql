syntax = "proto3";

package import_package;

service ServiceImport {
    rpc getQueryImportMethod (Name) returns (Empty);
    rpc mutationImportMethod (Name) returns (Empty);
}

message Name {
    string name = 1;
}

message Empty {

}